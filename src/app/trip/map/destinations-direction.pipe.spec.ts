import { DestinationsDirectionPipe } from './destinations-direction.pipe';

describe('DestinationsDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new DestinationsDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
