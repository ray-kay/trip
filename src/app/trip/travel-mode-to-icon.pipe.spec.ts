import { TravelModeToIconPipe } from './travel-mode-to-icon.pipe';

describe('TravelModeToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TravelModeToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
